name: action
on: push
jobs:
  nodejs:
    runs-on: ubuntu-latest
    steps:
      - name: pull_code
        run:  actions/checkout@v7
      - name: node version
        uses: actions/setup-node@v7
        with:
          node-version: '18' 